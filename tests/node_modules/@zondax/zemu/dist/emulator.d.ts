export declare const DEV_CERT_PRIVATE_KEY = "ff701d781f43ce106f72dc26a46b6a83e053b5d07bb3d4ceab79c91ca822a66b";
export declare const BOLOS_SDK = "/project/deps/nanos-secure-sdk";
export declare const DEFAULT_APP_PATH = "/project/app/bin";
export default class EmuContainer {
    private logging;
    private readonly elfLocalPath;
    private readonly name;
    private readonly image;
    private readonly libElfs;
    private currentContainer?;
    constructor(elfLocalPath: string, libElfs: Record<string, string>, image: string, name: string);
    static killContainerByName(name: string): void;
    static checkAndPullImage(imageName: string): void;
    log(message: string): void;
    runContainer(options: {
        logging: boolean;
        custom: string;
        model: string;
        transportPort: string;
        speculosApiPort: string;
    }): Promise<void>;
    stop(): Promise<void>;
}
